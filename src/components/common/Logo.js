import React from 'react';
import styled from 'styled-components';

const SLogo = styled.h1`
  font-size: 2rem;
  font-weight: 700;
`;

const Logo = () => {
  return <SLogo>Swim</SLogo>;
};

export default Logo;
